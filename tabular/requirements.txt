# Training environment for the tabular engine (inference deps are pinned
# separately into each exported artifact's own requirements.txt).
# These are floor constraints, not pins: the committed demo runs satisfied
# them (see the Reproducibility note in the README), and the test suite is
# the compatibility contract for newer versions.
scikit-learn>=1.6,<2
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
joblib>=1.3
scipy>=1.11
# model libraries (the engine degrades gracefully if one is missing)
xgboost>=2.0
lightgbm>=4.2
catboost>=1.2
# tuning + explanations
optuna>=3.5
shap>=0.45
# parquet support
pyarrow>=15
# demo notebooks / tests
jupyter
pytest
