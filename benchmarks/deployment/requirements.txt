# Deployment benchmark = the serving stack + an HTTP client.
# The engine itself is NOT needed: measure.py talks to an already-exported
# artifact over HTTP, which is the property being measured.
-r ../../serving/requirements.txt
httpx>=0.27
pandas>=2.0
pyarrow>=15          # reading the cached benchmark dataset for request bodies
